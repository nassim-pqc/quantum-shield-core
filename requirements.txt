# --- Web Framework & API ---
fastapi==0.133.1
# starlette is a transitive dep of fastapi; pinned explicitly so security
# advisories are tracked deterministically. Bumped to 1.3.1 to clear
# GHSA-82w8-qh3p-5jfq and GHSA-jp82-jpqv-5vv3; fastapi 0.133.1 allows
# starlette>=0.40.0, so this is in range.
starlette==1.3.1
uvicorn[standard]==0.30.1
python-multipart==0.0.32

# --- Data Validation ---
pydantic==2.7.4

# --- Cryptography ---
# Bumped to 48.0.1 to clear GHSA-537c-gmf6-5ccf.
cryptography==48.0.1
liboqs-python==0.14.1

# --- Database (async ORM) ---
sqlalchemy==2.0.31
aiosqlite==0.20.0
asyncpg==0.29.0
alembic==1.13.2

# --- Observability & Security ---
slowapi==0.1.9
prometheus-fastapi-instrumentator==8.0.0
prometheus-client==0.20.0
opentelemetry-api==1.25.0
opentelemetry-sdk==1.25.0
opentelemetry-instrumentation-fastapi==0.46b0
opentelemetry-exporter-otlp-proto-grpc==1.25.0

# --- HTTP Client (for Vault, KMS integrations) ---
httpx==0.27.0
boto3==1.34.128
botocore==1.34.128
hvac==1.2.1
azure-keyvault-secrets==4.7.0
azure-keyvault-keys==4.11.1
azure-identity==1.16.1

# --- Retry (KMS providers) ---
tenacity==8.5.0

# --- Config ---
python-dotenv==1.2.2
