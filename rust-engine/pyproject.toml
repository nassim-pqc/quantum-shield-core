[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "quantum-shield-engine"
version = "0.1.0"
requires-python = ">=3.11"

[tool.maturin]
module-name = "quantum_shield_engine"